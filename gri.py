"""
Основные правила интерпретации (ОПИ) ТН ВЭД ЕАЭС.
Краткое содержание — для подсветки в результате и подсказок LLM.

Полный текст ОПИ — в Приложении к ТН ВЭД ЕАЭС (Решение Совета ЕЭК).
"""

GRI_RULES: dict[str, str] = {
    "1":  "Классификация по текстам товарных позиций и примечаниям к разделам и группам.",
    "2a": "Незавершённые/некомплектные товары классифицируются как готовые, если имеют их основные свойства.",
    "2b": "Смеси и соединения с прочими материалами рассматриваются по основному материалу или компоненту.",
    "3a": "Из нескольких подходящих позиций выбирается наиболее конкретно описывающая товар.",
    "3b": "Смеси и многокомпонентные товары классифицируются по компоненту, придающему основное свойство.",
    "3c": "Если 3а и 3б не применимы — позиция с бо́льшим (последним по возрастанию) кодом.",
    "4":  "Если ни одна позиция не подходит — товар, наиболее сходный по свойствам.",
    "5a": "Футляры и чехлы, поставляемые с товаром, классифицируются вместе с товаром.",
    "5b": "Упаковка классифицируется вместе с товаром (если не предназначена для повторного использования).",
    "6":  "Классификация в субпозициях ведётся по их наименованиям; сравниваются только субпозиции одного уровня.",
}


def gri_text(code: str) -> str:
    """Полное описание правила по короткому коду (например, '3a')."""
    code = code.lower().replace("опи", "").replace(" ", "").strip()
    return GRI_RULES.get(code, "")


GRI_HINT_FOR_PROMPT = (
    "Основные правила интерпретации (ОПИ):\n"
    + "\n".join(f"  ОПИ {k}: {v}" for k, v in GRI_RULES.items())
)
