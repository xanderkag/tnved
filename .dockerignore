__pycache__/
*.pyc
*.pyo
.Python
.git/
.gitignore
.venv/
venv/

# секреты НЕ в образ
.env
.env.*
!.env.example

# raw-источники (CSV+xlsx) не нужны в образе — данные уже в SQLite/FAISS
data/raw/
.hf-cache/

# !! ВАЖНО: data/tnved.db, data/tnved.faiss, data/tnved_meta.json
# теперь ИДУТ В ОБРАЗ — собраны локально и копируются через build context.
# Если их нет — Dockerfile переключается на fallback (медленный) build_index.

# демо-сервер и фикстуры — только локально для превью без LLM
demo_server.py
demo_input.xlsx
static/demo_input.xlsx

# legacy / dev
.streamlit/
.claude/
*.log
*.bak
.DS_Store
Thumbs.db
.idea/
.vscode/
