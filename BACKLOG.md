# Бэклог ТН ВЭД Ассистента

**Дата:** 25.09.2026 · **Статус:** черновик; решения D-1…D-8 приняты 25.09 ·
**Заменяет** `TECH_DEBT.md`: все 23 пункта и этапы C/D/E разнесены по карточкам (пометка `TD-N`), сводка — в §5.

Размер: **S** — до дня, **M** — 2–4 дня, **L** — неделя и больше.
Приоритет: **P0** — без этого дальше не идём, **P1** — до боевого запуска, **P2** — потом.

---

## 0. Рамки

**Что делаем.** Подсказку кода ТН ВЭД ЕАЭС с обоснованием. Решает декларант: «автоматически
определяем верный код» не обещаем (так договорились в позиционировании для SLAI).

**Принцип (D-8): в основе — алгоритмы и код, модель только помогает.** Код ведёт товар по дереву
справочника, как декларант по ОПИ 1 и 6: сравнивает соседние позиции по их условиям — плотность,
мощность, доля материала, «прочие» по исключению — и сам решает всё, что проверяется. Модель — там, где
нужен язык: разобрать свободное описание, выбрать между позициями, если правила оставили несколько,
изложить обоснование. Код не из допустимого набора она вернуть не может, её решения помечены в
обосновании. Без модели сервис выдаёт допустимые коды и вопросы.

**Один код — два инстанса.** Код общий, у каждого инстанса свой `.env` и свои данные — как у parsdocs.

| | Taipit (корпоративный контур) | Asha |
|---|---|---|
| Данные | реальные корп-данные — только здесь | корп-данных нет (правило хоста) |
| LLM | vLLM на GPU-хосте, OpenAI-совместимый API, без ключа: `qwen36-vllm` (Qwen3.6-27B, окно 32k) — на нём разбор и ассистент SLAI; `qwen38-vision` (Qwen3.8-27B) — отдельный vLLM под OCR сканов | облачная Qwen3.8-27B через шлюз Ванги, 4 млн токенов в сутки (**D-1**). Своей не поднять: CPU, 15 ГБ RAM, маршрута до GPU-хоста нет |
| Эмбеддер | e5-base на CPU | то же; по памяти влезает (модель ~1 ГБ, индекс ~80 МБ) |
| Сеть | закрытая: веса с Hugging Face не качаются, Google Fonts недоступны | открытая |

**Где идёт работа.** Машина разработки в Taipit не ходит (**D-7**). Здесь — код, публичный эталон и
прогоны на облачной модели через шлюз. Всё, что требует контура, делает параллельная сессия внутри
него по тому же репозиторию (**D-4**): локальные модели, корп-данные, развёртывание в Taipit. Наружу из
контура выходят только сводные метрики, без текстов описаний.

Источники: `Doc-Parser/CLAUDE.md` (таблица маршрутов LLM), `Doc-Parser/doc-service/docs/RUNTIME_TOPOLOGY.md`,
`Doc-Parser/docs/MODEL_REPORT.md` (замер 39), `SLAI/docs/specs/ai-channels-tz.md` §1.3.

⚠️ Топология меняется: Ollama с 08.09 переехала на другой хост. Порты vLLM parsdocs_dev подтвердил 25.09
(`Q-TN-VED-LOKALNYY-1`, ход 2): `qwen36-vllm` — :8100, `qwen38-vision` — :8101, :8000 из `DEPLOY_TOPOLOGY.md`
устарел. Но GPU-хост в тот день не отвечал и с их машины, поэтому сессия в контуре начинает с живой проверки
`/v1/models` и `/version`. Адресов по-прежнему нет ни в коде, ни здесь — только в `.env` инстанса.

---

## 1. Решения владельца — приняты 25.09

| # | Вопрос | Решение |
|---|---|---|
| D-1 | Откуда LLM на Asha | Облачная Qwen3.8-27B через шлюз Ванги, своя личность `tnved-bot` — как у AICRM (`aicrm-bot`) и Holodos (`holodos-bot`). Выдано 25.09 (`Q-TN-VED-DOSTUP-1`, ход 2): алиас `parsdocs-chat-qwen38-27b` (`qwen/qwen3.8-27b` в OpenRouter; провайдеры DeepInfra в bf16, Parasail и CoreWeave в fp8), 4 млн токенов в сутки со сбросом в 03:00 МСК, до $5 в сутки. Ключ — у владельца файлом (где лежит — в треде), в `.env` его кладёт владелец. По имени это та же модель, что `qwen38-vision` в Taipit; совпадают ли веса и квантование, покажет EVAL-3. Сейчас одна классификация — два вызова и 10–12 тыс. токенов, то есть лимита хватает примерно на 350 в сутки. Нет ключа или кончился лимит — сервис работает без модели (**D-8**) |
| D-2 | Кто главный потребитель | Скорее всего SLAI: массовая подсказка кодов по строкам инвойсов (из 29 691 строки у 70% кода нет, `SLAI/docs/backlog/described.md`). API первым, веб-интерфейс вторым. Контракт — `Q-TN-VED-KONTRAKT-1` |
| D-3 | Источник справочника | Официальный справочник из открытых источников, даже если его придётся разбирать. Он есть: PDF ЕЭК по группам (§2, DATA-1). Коммерческие системы — Sigma Soft (к ней есть доступ через знакомых брокеров), ТКС, Альта-Софт, STM — крайний случай и по минимуму. Даты изменений известны заранее, поэтому и тогда забираем прицельно к дате, без постоянного доступа |
| D-4 | Оценка на корп-данных | На базе Taipit, в параллельной сессии внутри контура. Данные контур не покидают, сюда приходят только сводные метрики |
| D-5 | Какая копия основная | `~/Tnved`. Данные и окружение (Python 3.11) перенесены сюда. `~/Desktop/TNVED` 25.09 отправлена в Корзину и восстанавливается, пока Корзину не очистили |
| D-6 | Свой id в AgentMail | `tnved_dev` заведён 25.09 (AgentMail, коммит `c37f2e0`), вопросы коллегам — §4 |
| D-7 | Откуда гонять локальные модели | С машины разработки доступа в Taipit нет и не будет. `qwen36-vllm` и `qwen38-vision` гоняет сессия внутри контура — та же, что в D-4 |
| D-8 | Роль модели | «Модель только помогает, в основе — алгоритмы и код». Сейчас наоборот: группу и код выбирает модель, а код только ищет по векторам (`classifier.py`). Разворот — этап 3: код ведёт по дереву справочника и решает всё, что проверяется условиями (ALG); у модели — места, где нужен язык (LLM-2), и каждое должно окупиться приростом на EVAL. Без модели сервис работает, а не падает |

---

## 2. Что проверено 25.09 — почему такой порядок

- **Данные старше редакции 2022 года.** В базе нет 8806 (беспилотники) и 851713 (смартфоны). Источник `infoculture/opencustoms` — 2017 год. (TD-1)
- **Официальный справочник открыт, но только в PDF.** ЕЭК публикует ТН ВЭД ЕАЭС вместе с ЕТТ: PDF на каждую группу, с текстовым слоем, не сканы. Пояснения — там же, тоже PDF по группам. Машиночитаемой версии на сайте нет. Проверил две группы: в 52-й 149 кодов, с нашей базой совпадают один в один; в 88-й есть 8806 — 59 кодов, у нас ни одного.
- **Верного кода нет среди кандидатов даже на простом запросе.** «Хлопковая ткань суровая, 150 г/м²»:
  - первым идёт 5208 21 100 0 — в его тексте прямо написано «ОТБЕЛЕННЫЕ» и «не более 100 г/м²»;
  - из 12 кандидатов ни одного неотбелённого кода, который подходит под 150 г/м² (5208 12, 13, 19), хотя в базе у 5208 12 есть «НЕОТБЕЛЕННЫЕ [СУРОВЫЕ]»;
  - каждый кандидат стоит дважды — 10- и 8-значный с одним текстом, так что различных позиций всего 6;
  - после очистки от дублей ближайшие 5208 12 — на 15-м и 18-м месте, а на 11-м — живой скот (0102).

  Модели не из чего выбрать. Вектор не различает ни признак, ни число — это работа для дерева и условий (ALG-3), а не для поиска. Запрос — первый кандидат в эталон.
- **Слова «суровая» в официальном тексте нет** — только «неотбеленные». В нашей базе оно есть лишь потому, что opencustoms пересказывает тексты. С переходом на PDF ЕЭК эта подсказка пропадёт — нужен словарь торговых терминов (ALG-2).
- **Большую часть различий между соседними кодами можно проверить кодом.** Грубая оценка по текущей базе (пересказ 2017 года), 12 480 десятизначных кодов:
  - у 27% в пути есть число — плотность, мощность, объём, доля, возраст;
  - 51% получены через «прочие», то есть исключением. Вектор исключение выразить не может: «прочие» для него — просто текст родителя;
  - хотя бы одно из двух — у 65%. Больше всего в пищепроме (84%), транспорте (76%), металлах (75%), текстиле (70%) и машинах (67%). Меньше всего в химии (42%) и растениях (40%): там коды различаются названием вещества или вида — это словари и поиск по словам.

  По официальному дереву пересчитаем в ALG-1.
- **32% индекса — дубли.** 8 618 из 27 168 векторов совпадают дословно. Причина: `parse_tnved.py` вешает 8- и 10-значные коды прямо на 6-значный (`parent_code`), и `full_path` у них одинаковый. (TD-21)
- **В кандидаты попадают 6- и 8-значные коды** (`build_index.py:43`, `level >= 3`), хотя модель просят выбрать 10-значный.
- **Группа из триажа — жёсткий фильтр** (`classifier.py:174`). Ошиблась модель с группой — верного кода среди кандидатов нет. Поиск без фильтра включается, только если в группе не нашлось ничего.
- **Код от модели не проверяется**: может не существовать в справочнике. (TD-2)
- **Нет разделов I–XXI и примечаний.** (TD-5)
- **Async только снаружи**: эмбеддинг и SQLite вызываются синхронно внутри async-обработчиков (`tnved_data.py:47`), один запрос держит остальные.
- **Имя модели приходит от клиента без проверки** (`api.py`) — в прод-инстансе так нельзя.
- **Модели по умолчанию `qwen2.5:14b` нет ни в одном контуре**, а клиент `ollama` с vLLM не работает.
- **Не готово к закрытой сети**: `index.html` тянет Google Fonts, эмбеддер при первом старте качается с Hugging Face, а при каждом следующем сверяется с ним. Замер: инициализация 7,9 с, с `HF_HUB_OFFLINE=1` — 2,0 с. В закрытой сети вместо этих секунд будут таймауты.
- **Нет тестов и эталонного набора** — выбор модели сейчас был бы на глаз. (TD-6)

---

## 3. Этапы и карточки

### Этап 1. Научиться мерить — P0

Без этого «посмотрим, потестируем» — это спор мнений. Сначала меряем конвейер как есть: это точка
отсчёта для всего остального. Дальше каждый прогон — в двух режимах, без модели и с моделью: разница
и есть её польза (**D-8**).

**LLM-1. OpenAI-совместимый клиент и конфиг инстанса** · S · TD-3
- `ollama` → `openai` (AsyncOpenAI): один клиент для vLLM в Taipit и шлюза Ванги.
- Всё из `.env`: `LLM_BASE_URL`, `LLM_MODEL` (на шлюзе — `parsdocs-chat-qwen38-27b`), `LLM_API_KEY`, `LLM_THINKING=off|on`, `LLM_THINKING_PARAM`, `LLM_TIMEOUT_S`. `qwen2.5:14b` из кода убрать.
- Ключ шлюза выдан 25.09 файлом владельцу, копия — на asha (`Q-TN-VED-DOSTUP-1`). В `.env` на хосте его кладёт владелец. `.env` в git не попадает (`.gitignore`), в переписку ключ не пишем — только где он лежит.
- Thinking выключается по-разному, а чужой параметр каждая сторона молча пропускает: рассуждения идут и тратят токены (ответы parsdocs 25.09):
  - шлюз — только `reasoning_effort:"none"`; `enable_thinking` OpenRouter не знает (`Q-TN-VED-DOSTUP-1`);
  - vLLM Taipit — только `chat_template_kwargs.enable_thinking=false`; `reasoning_effort` vLLM для Qwen не читает (`Q-TN-VED-LOKALNYY-1`).

  Поэтому способ выключения — настройка инстанса (`LLM_THINKING_PARAM`), а не код. Рассуждения должны приходить отдельно от ответа: на шлюзе — в `message.reasoning`; в vLLM проверить, что в `reasoning_content`, а не внутри `content`, иначе JSON не разберётся.
- Грабли шлюза (`Holodos/docs/VANGA_INTEGRATION.md`):
  - ответ обрезается молча: `finish_reason == "length"` — это ошибка, а не ответ; `max_tokens` не отправлять;
  - модель рассуждающая, рассуждение съедает бюджет ответа — thinking выключен по умолчанию;
  - запросы обслуживаются по одному: пакеты и прогоны EVAL — последовательно;
  - 401 — ключ, 403 `model_not_allowed` — не тот алиас, 429 — суточный лимит; 429 не повторять в цикле;
  - остаток лимита и время сброса — в заголовках `x-parsdocs-quota-*`: пишем в лог, при 429 показываем, когда сброс.
- Модель выбирается только из списка в конфиге: `/api/models` отдаёт этот список, чужое имя сервер отклоняет.
- Сломанный JSON — одна повторная попытка, потом понятная ошибка вместо 500.
- **Готово, когда** один и тот же код без правок ходит в облачную Qwen3.8-27B через шлюз (проверяем здесь) и в `qwen36-vllm` / `qwen38-vision` (проверяет сессия в контуре).

**EVAL-1. Эталонный набор** · M
- **Публичная часть** — лежит в репозитории: 300+ пар «описание → код», равномерно по разделам. Кандидаты в источники:
  - предварительные решения ФТС о классификации;
  - решения Коллегии ЕЭК о классификации отдельных товаров — у них есть и обоснование с ОПИ.

  Сначала проверить формат и объём.
- **Корп-часть** — база Taipit: живёт только внутри контура, в репозиторий не попадает (**D-4**). Кандидаты:
  - 1 196 строк инвойсов с кодами (INV-4b);
  - 4 323 товарные строки деклараций IN WIN (DECL-BASE).

  Где они лежат в контуре и в каких полях, спросил у slai_dev (`Q-TN-VED-KONTRAKT-1`, п. 5). Это настоящий
  язык инвойсов — коротко, с артикулами. На публичные решения он не похож, поэтому нужны оба набора.
- У каждой пары указаны источник (номер и дата решения) и редакция ТН ВЭД, по которой код верен: после смены редакции эталон пересчитывается по таблице соответствия.
- **Готово, когда** `eval/sets/public.jsonl` в репозитории и формат описан.

**EVAL-2. Прогон и метрики** · M · TD-6
- `eval/run.py`: набор × конфигурация → построчные результаты и сводка в `eval/reports/` (без клиентских данных).
- Метрики:
  - точность на 2, 4, 6 и 10 знаках;
  - попадание в топ-3 (основной код или альтернатива);
  - **полнота поиска** — есть ли верный код среди кандидатов вообще; это потолок для модели;
  - без модели (**D-8**): есть ли верный код среди допустимых, сколько кодов осталось, сколько вопросов до одного кода, доля строк, решённых без модели;
  - с моделью — прирост на каждом её месте (LLM-2) и токены на строку;
  - доля несуществующих кодов и сломанного JSON;
  - задержка p50/p95 по этапам, токены;
  - отдельно — «эталонного кода нет в справочнике»: на старых данных таких будет много.
- В отчёт пишем не только имя модели, но и что за ним стоит (`/v1/models`). У vLLM Taipit на одном порту два имени ведут в одну модель (`RUNTIME_TOPOLOGY.md`) — без этого легко сравнить модель саму с собой. На шлюзе — ещё и провайдера из заголовка `x-parsdocs-upstream-provider`: DeepInfra отвечает в bf16, Parasail и CoreWeave — в fp8, и ответы немного различаются.
- Неинтерактивный режим: на вопросы никто не отвечает, классификация идёт по исходному описанию — как у SLAI.
- Прогон переносим: одна команда, конфиг из `.env`, наружу ходит только в LLM. Для корп-набора в отчёт
  идут метрики и номера строк, без текстов описаний, — такой отчёт можно вынести из контура.
- Режим с «оракулом» (P2): на вопросы — сейчас триажа, после ALG-4 из дерева — отвечает модель, у которой есть полный текст решения. Так меряется польза вопросов.
- Юнит-тесты на разбор справочника, иерархию и проверку кода. В CI — дымовой прогон на 10 примерах с заглушкой вместо LLM.
- **Готово, когда** одна команда выдаёт сводную таблицу для любой конфигурации.

**EVAL-3. Точка отсчёта: матрица моделей** · S
- Здесь: облачная Qwen3.8-27B через шлюз × thinking вкл/выкл на публичном наборе. Полный прогон — порядка
  7 млн токенов, почти два суточных лимита (4 млн); согласуем с владельцем отдельно.
- В контуре (сессия D-4): `qwen36-vllm` и `qwen38-vision` × thinking вкл/выкл на публичном и корп-наборе.
  Окно — с 19:00 до 09:00 МСК и выходные: днём vLLM заняты пилотами и ассистентом SLAI (`Q-TN-VED-LOKALNYY-1`).
  Длительность прикидываем по первым 20 вызовам; не влезает в ночь — по модели на ночь.
- Публичный набор общий, поэтому облачную и локальную Qwen3.8-27B сравниваем напрямую — если совпадают веса и
  квантование: у `qwen38-vision` смотрим `/v1/models` (root, max_model_len), у шлюза — провайдера.
- Для боя в Taipit естественный кандидат — `qwen36-vllm`: на нём уже классификация parsdocs, а `qwen38-vision` —
  отдельный vLLM под OCR сканов.
- После этапа 3 модели сравниваем не по выбору кода целиком, а по приросту на их местах (LLM-2).
- Итог — отчёт с рекомендацией модели и режима для каждого инстанса: точность против задержки.
- Повторяется после каждого этапа ниже. Карточки этапов 2–3 принимаются по приросту метрик.

### Этап 2. Дерево справочника — P0

Алгоритму (этап 3) нужно официальное дерево целиком: строки без кода («– неотбеленные:»), «прочие» и
условия в том виде, как их пишет ЕЭК. Пересказ opencustoms для этого не годится: у 5208 12 160 0
«более 100 г/кв.м», у соседнего 5208 12 190 0 — «выше 100 г/кв.м», у родителя — «г/м.кв.».

**DATA-1. Спайк: откуда брать справочник** · S · TD-1
- **Найдено 25.09** — страница ЕЭК «ТН ВЭД ЕАЭС и ЕТТ ЕАЭС» (`eec.eaeunion.org/comission/department/catr/ett/`):
  - PDF на каждую группу 01–97; в имени файла — дата последней поправки (группа 52 — 08.03.2026);
  - в строке кода — доп. единица измерения и ставка ввозной пошлины;
  - отдельно — ОПИ, «Примечания к ТН ВЭД» и «Примечания к ЕТТ»;
  - текст достаётся без OCR (pypdf).
- Осталось проверить:
  - нет ли машиночитаемой версии в НСИ ФТС (классификаторы для разработчиков ПО) — сэкономила бы разбор;
  - где примечания к разделам — в PDF первой группы раздела или отдельно;
  - что в отдельных «Примечаниях к ТН ВЭД».
- Коммерческие системы (Sigma Soft, ТКС) — крайний случай и по минимуму (**D-3**): только то, чего в
  открытом доступе нет или что оттуда не достать разумными силами. Если дойдёт — смотрим:
  - что внутри: полный текст с примечаниями, Пояснения, решения о классификации, даты действия;
  - формат выгрузки;
  - что условия доступа разрешают встроить в сервис — особенно в asha, который смотрит в интернет.
- Критерии:
  - официальность;
  - полные, не сокращённые наименования;
  - даты действия;
  - примечания;
  - частота обновлений;
  - лицензия.
- ⚠️ С 01.01.2027 вступает новая редакция Гармонизированной системы, ГС-2027. ЕАЭС обычно переходит синхронно, так что через три месяца справочник снова сменится (сроки ЕЭК проверить). Нужен конвейер загрузки, а не разовый импорт.
- **Готово, когда** источник выбран, а образец данных лежит в `data/samples/`.

**DATA-2. Конвейер загрузки с версиями** · M · TD-1, TD-21, TD-22
- У каждого кода: `edition`, `valid_from`, `valid_to`. Несколько редакций живут рядом; по умолчанию берётся действующая на дату запроса.
- Разбор PDF ЕЭК (DATA-1). Что видно на группах 52 и 88:
  - уровень строки задают тире, а не длина кода: есть строки без кода («– прочие:», «– неотбеленные:»)
    и 9-значные уровни (`8806 10 001`). Родитель — ближайшая строка выше с меньшим числом тире;
  - наименование переносится на несколько строк, в конце строки — единица измерения и ставка;
  - на каждой странице повторяется шапка таблицы — выбрасывать;
  - надёжнее разбирать по координатам (pdfplumber), чем по сплошному тексту.
- Ставку пошлины и доп. единицу сохраняем сразу: они в той же строке, это задел на тариф.
- Разделы I–XXI.
- Новая версия данных подключается без правки кода, сервис подхватывает её без рестарта.
- Грузим прицельно к дате, а не опросом по расписанию: поправки в ТН ВЭД выходят решениями ЕЭК, дата
  вступления в силу известна заранее. Новую редакцию загружаем до этой даты, включается она сама по `valid_from`.
- **Готово, когда** 8806 и 851713 на месте, число 10-значных кодов по каждой группе совпадает с PDF, дублей `full_path` нет, строки без кода — узлы дерева, а текст каждого узла — как в PDF.

**DATA-3. Примечания к разделам и группам** · M · TD-5
- Таблица `notes`: раздел или группа → текст, плюс дополнительные примечания ЕАЭС.
- Источник — начало PDF каждой группы (у 52-й там «Примечание к субпозициям»). Примечания к разделам и
  отдельный PDF «Примечания к ТН ВЭД» — по итогам DATA-1.
- Для алгоритма в примечаниях два слоя: определения признаков и исключения из групп. Из них — правила ALG-6.
- **Готово, когда** у каждой из 96 групп есть примечания — если они есть в официальном тексте.

**DATA-4. Пояснения к ТН ВЭД** · L · P2
- Официальные пояснения к позициям: большой документ и самый ценный источник обоснований, а заодно торговых терминов для словаря (ALG-2). Нужен поиск по ним на этапе классификации.
- Источник — PDF ЕЭК по группам (`eec.eaeunion.org/comission/department/catr/psn/`), разбор — как в DATA-2. Коммерческая выгрузка — только крайний случай (**D-3**).
- Берёмся, если этап 3 покажет, что одних примечаний мало.

### Этап 3. Алгоритм и точность — P0/P1

Каждая карточка — эксперимент: прогон EVAL до и после.

Порядок (**D-8**): сначала ядро — дерево и условия решают всё, что проверяется (ALG); затем поиск, который
подаёт ядру позиции-кандидаты (SRCH); и только потом места для модели (LLM-2). Начинаем со сквозного среза
на одной группе — 52-й: дерево из PDF → условия → навигатор → вопросы. Держит полноту на ней — расширяем на все.

**ALG-1. Условия из текстов узлов** · M · P0
- У каждого узла дерева — условия, которыми он отличается от соседей:
  - числа: «не более 100 г/м²», «более 100 г/м², но не более 130 г/м²», «85 мас.% или более», «шириной более 165 см» → признак, единица, границы;
  - значения признаков: неотбеленные / отбеленные / окрашенные, вид переплетения, новые / бывшие в эксплуатации, «для промышленной сборки»;
  - «прочие» — не условие, а исключение: подходит, если не подошёл ни один сосед.
- Разбираем правилами: обороты и единицы в официальном тексте однотипны. Результат хранится данными, а не кодом. Сложный текст может разобрать модель, но в данные разбор попадает только после проверки (LLM-2).
- Проверка на самих данных: границы соседей стыкуются без дыр и нахлёстов (у 5208 11 «не более 100 г/м²», у 5208 12 «более 100 г/м²»).
- **Готово, когда** по каждому разделу известна доля разобранных узлов (оценка по старой базе — §2), а три группы из разных разделов проверены целиком.

**ALG-2. Признаки товара из описания** · M · P0
- Сначала код: числа с единицами (150 г/м², 1,5 кВт, 1 600 см³, 85%), словари материалов и обработки, словарь торговых терминов: «суровая» → «неотбеленная», «х/б» → «хлопчатобумажная», «нерж.» → «коррозионностойкая».
- Без словаря не обойтись: слова «суровая» в официальном тексте нет (§2), и с переходом на PDF ЕЭК поиск его потеряет. Источники словаря — Пояснения (DATA-4), прецеденты (ALG-5), черновики модели после проверки.
- Чего код не нашёл, дозаполняет модель (LLM-2) — только признаки, которые разделяют оставшиеся ветки (их перечень даёт ALG-3), по жёсткой схеме. Значения проверяет код: единицы, диапазоны.
- **Готово, когда** на эталоне известны доля признаков, извлечённых без модели, и точность извлечения.

**ALG-3. Навигатор по дереву** · L · P0
- На входе описание и признаки. На выходе — допустимые 10-значные коды, вопросы и путь: какое условие на каком уровне сработало.
- Шаг 1 — позиции-кандидаты (4 знака): поиск по словам и векторам (SRCH-5), прецеденты (ALG-5), исключения из примечаний (ALG-6). Жёсткого фильтра по группе из триажа больше нет (§2). Если правила оставили несколько позиций, выбирает модель (LLM-2) с пометкой в пути; без модели в ответ идут все.
- Шаг 2 — спуск внутри позиции по ОПИ 6: на каждом уровне сравниваются только соседи. Условие выполнено у одного — идём вниз. Признак неизвестен — держим все подходящие ветки и запоминаем признак для вопроса. Не подошёл никто — «прочие».
- Режим без модели полноценный: те же допустимые коды, вопросы и путь.
- **Готово, когда** на «хлопковая ткань суровая, 150 г/м²» (§2) путь такой:
  - 150 г/м² оставляет позиции 5208, 5210 и 5212 (у 5209 и 5211 — более 200 г/м²); между ними решает доля хлопка — вопрос;
  - при хлопке 85% и более: 5208 → «неотбеленные» (словарь: «суровая») → 150 г/м² отсекает 5208 11;
  - переплетение не указано — 5208 12, 13 или 19, вопрос;
  - для полотняного 150 > 130 г/м² — 5208 12 960 0 или 990 0, вопрос о ширине (165 см).

  Итог при хлопке от 85% — 4 кода (5208 12 960 0, 5208 12 990 0, 5208 13 000 0, 5208 19 000 0) и вопросы, которые их различают. У нынешнего поиска среди 12 кандидатов верных кодов ноль.

**ALG-4. Вопросы из дерева** · S · P0 · TD-7, TD-14
- Вопрос строит навигатор: признак, по которому расходятся оставшиеся ветки; варианты ответа — из условий соседей («полотняное / 3- или 4-ниточное саржевое / другое»; «не более 165 см / более 165 см»).
- Первым — вопрос, который отсекает больше кодов. Признак уже известен — вопроса нет, так что повторов не бывает. Модель может переформулировать вопрос, но не придумывает его.
- Для SLAI вопросы уходят в ответ подсказкой, без диалога: «уточните ширину: 5208 12 960 0 или 990 0» (INT-1).
- Заменяет SRCH-7: таблицу вопросов по группам вести руками не нужно.
- **Готово, когда** вопросы строит навигатор, а на эталоне известно, сколько вопросов нужно до одного кода.

**ALG-5. Прецеденты** · M · P1
- База «описание → код» с источником и редакцией:
  - публичная — решения ЕЭК и предварительные решения ФТС о классификации;
  - корп — строки инвойсов и деклараций, только в контуре (**D-4**); сюда же коды, принятые декларантом (INT-3).
- Прецеденты — ещё один источник позиций-кандидатов для ALG-3 и торговых терминов для ALG-2. Для SLAI, вероятно, главный: у импортёра товары повторяются из поставки в поставку — проверим на корп-наборе.
- Эталон и прецеденты не пересекаются — делим по источнику или дате, иначе метрики завышены.
- **Готово, когда** на эталоне известен прирост от прецедентов.

**ALG-6. Правила из примечаний** · M · P1 · TD-5
- Исключения («в данную группу не включаются: …») → правило «такой товар — не сюда, а в такую-то позицию».
- Определения → смысл признаков для ALG-1 и ALG-2. Пример: в примечаниях к субпозициям раздела XI определены неотбеленная, отбеленная и окрашенная ткань и полотняное переплетение.
- Как в ALG-1: правила хранятся данными, черновик может предложить модель, в данные — после проверки. Зависит от DATA-3.
- **Готово, когда** правила есть для групп, где на EVAL чаще всего ошибаемся с позицией.

**SRCH-1. Поиск ищет позицию, а не код** · S
- Код внутри позиции находит дерево (ALG-3). Поэтому индекс — по узлам дерева без дублей текста, а попадания сворачиваются до позиций с путём до найденного узла. Сейчас половина из 12 мест — дубли (§2).
- **Готово, когда** дублей нет и измерена полнота по позициям: есть ли верная позиция среди кандидатов.

**SRCH-3. Код только из допустимых** · S · TD-2, TD-19
- Структурированный вывод: поле `code` — перечисление кодов из допустимого набора навигатора (`response_format` с json_schema).
  - Шлюз пропускает json_schema со strict и enum — проверено parsdocs 25.09 (`Q-TN-VED-DOSTUP-1`). Ответ приходит с пробелами и переносами: разбирать как JSON, не сравнивать строкой.
  - vLLM Taipit должен, но на этих портах json_schema не запускали; `guided_json` устарел. До прогона — пробный вызов с enum (`Q-TN-VED-LOKALNYY-1`).
- Плюс проверка на сервере: кода нет в справочнике — это ошибка, а не ответ.
- Заодно закрывается инъекция вида «верни код 0000000000»: код не из списка модель вернуть не может.
- **Готово, когда** доля несуществующих кодов — ноль.

**SRCH-4. Примечания в промпте** · M · TD-5
- Когда модель выбирает между позициями (LLM-2), ей подкладываются примечания их разделов и групп. Следить за окном: у `qwen36-vllm` оно 32k.
- Правила из примечаний — в ALG-6, здесь — текст для модели.
- **Готово, когда** точность выбора позиции растёт на случаях, где решают примечания.

**SRCH-5. Гибридный поиск** · S · P0
- FTS5 в SQLite по словам плюс векторы — источник позиций-кандидатов для ALG-3. Точные термины — «суровая», марки стали, названия веществ — вектор ловит плохо: на «суровая, 150 г/м²» он ставит первой отбелённую ткань (§2).
- Для русского нужен стеммер или поиск по началу слова: для FTS5 «суровая» и «суровые» — разные слова. Сюда же подключается словарь торговых терминов из ALG-2.

**SRCH-6. Выбор эмбеддера** · S · P2
- e5-base против e5-large и bge-m3 по полноте поиска. Ограничение — CPU и 15 ГБ на Asha.

**LLM-2. Места для модели** · M · P1
- Модель работает только там, где нужен язык. Каждое место включается отдельно:
  1. признаки, которые не взяли правила и словари (ALG-2);
  2. выбор между позициями, когда поиск, прецеденты и примечания оставили несколько (ОПИ 3), — с примечаниями групп в промпте (SRCH-4);
  3. обоснование человеческим языком по пути навигатора; по умолчанию — шаблон;
  4. вне запроса — черновики словарей, условий и правил для ALG-1, ALG-2, ALG-6; в данные они попадают после проверки.
- Код внутри позиции модель не выбирает, если его решают условия, и не возвращает код не из допустимого набора (SRCH-3).
- В пути каждое её решение помечено: декларант видит, где правило, а где мнение модели.
- Место остаётся, если на EVAL даёт прирост над режимом без модели; нет прироста — выключаем.
- Объём: в нынешнем виде лимита шлюза (4 млн токенов в сутки) хватает примерно на 350 классификаций. У SLAI около 20 тыс. строк без кода — это около двух месяцев лимита целиком. Поэтому модель зовём только для строк, которые код не решил, и с коротким промптом.
- **Готово, когда** по каждому месту есть замер прироста и токенов на строку.

Сняты 25.09 вместе с D-8: SRCH-2 (мягкий фильтр по группе) — в ALG-3 группы из триажа нет вовсе; SRCH-7 (таблица вопросов по группам) — вопросы строит ALG-4.

### Этап 4. Надёжный сервис — P1

**REL-1. Сессии в SQLite** · S · TD-4, TD-23
- Таблица сессий вместо словаря в памяти; сессия блокируется на время finalize.

**REL-2. Асинхронность и таймауты** · S · TD-13
- Эмбеддинг и SQLite — в пул потоков; таймаут на каждый вызов LLM берётся из конфига. Отмена запроса — P2.

**REL-3. Промпты в файлах, с версией** · S · TD-11
- `prompts/*.txt`. Версия промпта пишется в сессию и в отчёт EVAL, иначе прогоны несравнимы.

**REL-4. Логи и здоровье** · S · TD-12
- JSON-логи: модель, версия промпта и данных, задержки, токены.
- `/health`: отвечает ли LLM, загружены ли данные, какая редакция.

**REL-5. Мелочи кода** · S · TD-15
- Удалить Streamlit `app.py` и подсказку `streamlit run app.py` в `build_index.py`.
- `datetime.utcnow()` → `datetime.now(UTC)`.

### Этап 5. Два инстанса — P1

**OPS-1. Образ и конфиг** · M · TD-20
- Dockerfile + compose. Данные и веса эмбеддера (около 1 ГБ) — отдельным томом, не в образе: в Taipit образ не везут, а собирают на хосте из исходников (`Q-TN-VED-LOKALNYY-1`, ход 2), и сборка не должна ходить в Hugging Face. Веса едут вместе с данными (OPS-3).
- `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`: сервис не ходит в Hugging Face — минус 6 с на старте, а в закрытой сети нет таймаутов.
- Откуда на kb-docker при сборке берутся пакеты Python — зеркало PyPI или колёса с собой — уточнить в сессии внутри контура.
- `.env.example` со всеми ключами, без значений.

**OPS-2. Фронт без внешних запросов** · S
- Шрифты свои или системные вместо Google Fonts; страница не ходит наружу.

**OPS-3. Сборка и доставка данных** · S
- Данные публичные: собираем один раз снаружи, с номером версии, и раздаём обоим инстансам. Вместе с ними — веса эмбеддера (OPS-1).
- Как файлы попадают в контур, решает сессия внутри него: отсюда туда не дотянуться (**D-7**).

**OPS-4. Развёртывание в Taipit** · M
- Разворачивает сессия внутри контура: отсюда туда не дотянуться (**D-7**). С этой стороны — код, `.env.example` и инструкция.
- Куда и как — ответ parsdocs 25.09 (`Q-TN-VED-LOKALNYY-1`, ход 2):
  - хост kb-docker, рядом с parsdocs и gateway-litellm; не на GPU-машине;
  - код — в bare-репозиторий на kb-docker и в git-зеркало Taipit, сборка на хосте: `docker compose -p <проект> up -d --build`.
- Место и доступ решают владелец и админы Taipit. Адреса хостов и домен зеркала в бэклог не пишем: они в треде и в `.env` внутри контура.
- Нагрузку на общий vLLM согласовать: он же обслуживает parsdocs и ассистента SLAI. Массовые прогоны EVAL — в окно 19:00–09:00 МСК и в выходные.

**OPS-5. Развёртывание на Asha** · M
- LLM — облачная Qwen3.8-27B через шлюз Ванги (**D-1**). Бюджет памяти — 15 ГБ на весь хост.
- Ключ на asha parsdocs уже положил (где — `Q-TN-VED-DOSTUP-1`, ход 2). В контейнер он попадает через `.env` или файлом; в образ и git — никогда.
- Суточный лимит ключа — 4 млн токенов — общий на всех пользователей инстанса. Кончился — сервис переходит в режим без модели и говорит об этом, с временем сброса из заголовков шлюза, а не отдаёт 500 (**D-8**).
- На странице предупреждение: данные Taipit сюда не вводить.
- SSH на asha — разово и по явному разрешению владельца.

**OPS-6. Защита публичного инстанса** · M · TD-16, TD-17, TD-18
- Asha смотрит в интернет. Нужны вход, ограничение частоты запросов, заголовки безопасности и HTTPS через существующий прокси.

### Этап 6. Интеграция со SLAI — P1

**INT-1. Контракт API v1** · S
- Согласовать с slai_dev — `Q-TN-VED-KONTRAKT-1`.
- Один товар без диалога. В ответе:
  - допустимые коды; основной — если код один или его выбрала модель (с пометкой);
  - вопросы, которые различают оставшиеся коды (ALG-4), — подсказкой для человека, а не шагом диалога;
  - путь: какое условие решило на каждом уровне и где решала модель;
  - флаг `needs_review` и редакция справочника.
- Вместо «уверенности» от модели — сколько кодов осталось и где решала модель: это проверяемо.
- Не дублировать «позже ТН ВЭД» из справочного слоя SLAI (DIGEST).

**INT-2. Пакетный режим** · M · этап E
- `POST /api/v1/classify/batch` для строк инвойса; в интерфейсе — загрузка Excel или CSV.
- Пакет сначала идёт без модели; модель зовём только для строк, которые код не решил (LLM-2): на весь пакет лимита шлюза не хватит.

**INT-3. Обратная связь** · M
- Код, который декларант принял или исправил, сохраняется внутри Taipit и пополняет клиентский эталон.

### Этап 7. Интерфейс — P2

- **UX-1.** История классификаций с поиском · M · TD-8
- **UX-2.** Экспорт: JSON сессии, текст для декларации, PDF с обоснованием · M · TD-9
- **UX-3.** Правка ответов и группы после результата · S · TD-10
- **UX-4.** В результате — путь по дереву с пометками, где решала модель, редакция справочника и строка «решение за декларантом» · S

### Потом — P2

- Дообучить эмбеддер на парах из INT-3 (этап D).
- Классификация по фото через `qwen38-vision`: в Taipit модель уже стоит (этап E).
- Ставки пошлины и НДС в ответе: пошлина уже будет в базе после DATA-2, для НДС нужен свой источник (этап E).
- Описания на английском (этап E).
- Интеграции с Альта-Софт, ВЭД-Софт (этап E).
- Промпты под группы (этап D) — если на EVAL модель на своих местах ошибается по группам.

---

## 4. Переписка и что делаем, пока ждём

Заведены 25.09 в AgentMail от `tnved_dev`:

| Тред | Кому | О чём | Состояние | Разблокирует |
|---|---|---|---|---|
| `Q-TN-VED-DOSTUP-1` | parsdocs_dev | личность `tnved-bot` и ключ, псевдоним модели, лимит, json_schema и thinking на шлюзе | ответ 25.09 (ход 2); закрою после живого вызова — нужен ключ в `.env` | проверку LLM-1, EVAL-3 здесь, SRCH-3 |
| `Q-TN-VED-LOKALNYY-1` | parsdocs_dev | порты vLLM в Taipit, структурированный вывод, thinking, окно для прогонов, куда ставить сервис | ответ 25.09 (ход 2), принят | EVAL-3 в контуре, SRCH-3, OPS-4 |
| `Q-TN-VED-KONTRAKT-1` | slai_dev | сценарий и контракт API, объёмы, граница с DIGEST, где в контуре пары «описание → код» | ждём до 02.10 | INT-1, INT-2, корп-часть EVAL-1 |

Ключ пришёл владельцу файлом 25.09 (secret-ref — `Q-TN-VED-DOSTUP-1`, ход 2); в `~/Tnved/.env` его кладёт владелец.

**Не ждёт ответов:**
- DATA-2 на группе 52 — разбор PDF ЕЭК в дерево;
- ALG-1, ALG-3, ALG-4 — сквозной срез на группе 52;
- EVAL-1, публичная часть, и EVAL-2 в режиме без модели;
- LLM-1 — код пишется без ключа, проверка на шлюзе — когда ключ ляжет в `.env`;
- DATA-1 — источник найден, остались хвосты: НСИ ФТС и примечания к разделам;
- SRCH-1 и SRCH-5 — только данные и код.

---

## 5. Куда ушёл TECH_DEBT.md

| TD | Карточка | TD | Карточка |
|---|---|---|---|
| 1 | DATA-1, DATA-2 | 13 | REL-2 |
| 2 | SRCH-3 | 14 | ALG-4 |
| 3 | LLM-1 | 15 | REL-5 |
| 4 | REL-1 | 16–18 | OPS-6 |
| 5 | DATA-3, ALG-6, SRCH-4 | 19 | SRCH-3 |
| 6 | EVAL-1, EVAL-2 | 20 | OPS-1 |
| 7 | ALG-4 | 21 | DATA-2 |
| 8 | UX-1 | 22 | DATA-2 |
| 9 | UX-2 | 23 | REL-1 |
| 10 | UX-3 | Этап C | этапы 1–5 |
| 11 | REL-3 | Этап D | EVAL-1, DATA-4, «Потом» |
| 12 | REL-4 | Этап E | INT-2, «Потом» |
